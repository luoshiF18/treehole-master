<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.treehole.member.mapper.ThMenuMapper">

    <select id="selectPermissionByUserId" resultType="com.treehole.framework.domain.member.ThMenu" parameterType="java.lang.String" >
        SELECT
        id,
        CODE,
        p_id pid,
        menu_name menuName,
        url,
        is_menu isMenu,
        LEVEL,
        sort,
        STATUS,
        icon,
        create_time createTime,
        update_time updateTiem
        FROM
        th_menu
        WHERE id IN(
        SELECT menu_id FROM th_permission WHERE role_id IN(
        SELECT role_id FROM member_user WHERE user_id = #{id}
        )
        )

    </select>
</mapper>