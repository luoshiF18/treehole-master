<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.treehole.member.mapper.UserVoMapper">
    <select id="getAllUser" resultType="com.treehole.framework.domain.member.Vo.UserVo">
        SELECT
        user_id,
        user_name,
        user_nickname,
        gender,
        user_birth,
        user_phone
        FROM member_user
        WHERE user_id IN
        <foreach collection="list" item="listUserId" index="index" open="(" close=")" separator=",">
            #{listUserId}
        </foreach>
    </select>

    <select id="getUserByTime" resultType="com.treehole.framework.domain.member.User">
      SELECT
      *
      FROM member_user u
      WHERE u.user_createtime > #{before} and u.user_createtime &lt; #{after}
      AND u.role_id = "1"

    </select>
    <select id="getUserByCid" parameterType="java.lang.String" resultType="com.treehole.framework.domain.member.Vo.UserVo">
      select
      u.user_id,
          u.user_nickname,
          u.user_name,
        u.gender,
        u.user_phone,
        c.name as companyName
        from
        member_user u
        left join member_company c on u.company_id=c.id
        where
        c.id=#{companyId}


    </select>
</mapper>

